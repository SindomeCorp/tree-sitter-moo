# Roadmap

Near-term follow-up work:

- Add a LambdaMOO strict dialect mode or compatibility report.
- Expand `queries/moo/tags.scm` for better symbol extraction.
- Add locals/scope queries for editor and LLM tooling.
- Add indentation queries for editors that consume them.
- Validate against more real-world ToastStunt and LambdaMOO verb exports.
- Investigate a Zed extension.
- Investigate a VS Code extension or TextMate grammar fallback.
- Add release automation for npm/crates/Python packages if needed.
